# Growth OS — A/B Тестирование

## ⚡ Быстрый старт (3 шага)

### Шаг 1: Откройте новый терминал в VS Code

Используйте `Ctrl+Shift+`` (или `Cmd+Shift+`` на Mac) для открытия нового терминала

### Шаг 2: Запустите сервер

```bash
npm run serve
```

Сервер автоматически инициализирует БД и создаст демо-данные при первом запуске!

### Шаг 3: Откройте приложение

Откройте http://localhost:5173 в браузере

## 🔐 Вход

- **Логин**: admin
- **Пароль**: 123

После входа вы увидите 4 демо A/B теста с картинками

## 📝 Все команды

| Команда | Что делает |
|---------|-----------|
| `npm run dev` | Фронтенд на 5173 (уже запущен) |
| `npm run serve` | Сервер API на 5000 |
| `npm run seed` | Пересоздать демо-данные |
| `npm run seed:check` | Проверить пользователей в БД |
| `npm run db:push` | Пушить схему в Postgres |

## 🏗️ Архитектура

- **Фронтенд**: React 18 + TypeScript + Tailwind CSS + Wouter
- **Бэкенд**: Express + Drizzle ORM + PostgreSQL (Neon)
- **Реал-тайм**: React Query для синхронизации данных
- **Иконки**: Lucide React
- **UI**: Radix UI компоненты

## 📦 Структура проекта

```
growth-os/
├── client/              # React приложение
│   ├── src/
│   │   ├── pages/       # Страницы
│   │   ├── lib/
│   │   │   ├── components/  # Компоненты
│   │   │   ├── auth-context.tsx  # Авторизация
│   │   │   └── queryClient.ts    # React Query
│   │   └── App.tsx
│   └── index.html
├── server/              # Express сервер
│   ├── index.ts         # Главный файл
│   ├── routes.ts        # API маршруты
│   ├── db.ts            # БД подключение
│   └── seed-force.ts    # Инициализация данных
├── shared/              # Shared типы
│   └── schema.ts        # Drizzle ORM схема
└── vite.config.ts       # Vite конфиг (прокси на API)
```

## ⚙️ Переменные окружения

`.env` файл содержит:
- `DATABASE_URL` — PostgreSQL (Neon облако)
- `CLOUDINARY_CLOUD_NAME` — CDN для изображений

## 🚀 Что было сделано

✅ Исправлены пути импорта (@/ и @shared/)
✅ Создан tsconfig.json с path mappings
✅ Добавлен дефолтный QueryFn в React Query
✅ Автоматическая инициализация БД при запуске сервера
✅ Демо-данные с 4 A/B тестами и картинками
✅ Коррекция импортов в use-toast.ts

## 🎯 После входа вы сможете:

- ✅ Видеть список всех A/B тестов на доске
- ✅ Каждая карточка содержит: превью, статус, платформу, тип теста
- ✅ Информация об авторе (создателе теста)
- ✅ Кнопки для редактирования и просмотра деталей
- ✅ Кнопка "В LAB" для запуска теста